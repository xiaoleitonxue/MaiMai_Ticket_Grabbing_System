server:
  port: 8081
feign:
  okhttp:
    enabled: true
  sentinel:
    enabled: true
mai:
  db:
    database: mai-item
  swagger:
    title: "商品服务接口文档"
    package: com.mai.item.controller
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8081
seata:
  data-source-proxy-mode: AT